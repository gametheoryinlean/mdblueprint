# Extraction Comparison Experiment Plan

Run both extraction methods on the same small Lean sample set.

## Compare

- theorem-name recall
- dependency recall
- mapping ambiguity
- setup friction
- obvious false positives
- effort to map one authored node to multiple Lean theorems

## Expected output

- a short repository-review note describing where `lean-lsp-mcp` adds value over
  heuristic LLM extraction
- a countercheck report listing drift, warnings, and missing edges
